extends layout

block content
	h1= title
	p= 'last view flight' + lastNumber
	ul
		each flight, index in arrivals
			- landed = new Date(flight.actualArrive)
			li= flight.number + ': ' + flight.origin + '-' + landed
